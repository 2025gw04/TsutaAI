<Window x:Class="TsutaAI.Windows.GrowthTrackingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Style="{StaticResource BaseWindowStyle}"
        Title="成長トラッキング"
        Height="800"
        Width="1200"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="MetricCardStyle" TargetType="Border" BasedOn="{StaticResource Card}">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="20"/>
        </Style>

        <Style x:Key="GoalCardStyle" TargetType="Border" BasedOn="{StaticResource Card}">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- ========== ヘッダー ========== -->
        <Border Grid.Row="0" Background="{StaticResource WindowBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <!-- タイトル -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <iconPacks:PackIconBoxIcons Kind="RegularTrendingUp" Width="28" Height="28" HorizontalAlignment="Center" Margin="0,3,10,0"/>
                    <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                        <TextBlock Text="成長トラッキング" Style="{StaticResource H2}" FontSize="20"/>
                        <TextBlock x:Name="txtSubtitle" Text="あなたの成長を記録・分析します" Style="{StaticResource MutedTextStyle}"/>
                    </StackPanel>
                </StackPanel>

            </Grid>
        </Border>

        <!-- Content -->
        <TabControl Grid.Row="1" Margin="10,0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Border" Background="Transparent" BorderThickness="0,0,0,3" BorderBrush="Transparent" Padding="20,12" Margin="0,0,2,0">
                                    <StackPanel Orientation="Horizontal">
                                        <ContentPresenter ContentSource="Header" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                        <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#F3F4F6"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <!-- スキル成長タブ -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconBoxIcons Kind="RegularTrendingUp" Width="16" Height="16" Margin="0,0,8,0"/>
                        <TextBlock Text="スキル成長" FontWeight="SemiBold"/>
                    </StackPanel>
                </TabItem.Header>
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20" Background="{StaticResource BackgroundBrush}">
                    <StackPanel>
                        <Border Style="{StaticResource Card}" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="スキル成長履歴" Style="{StaticResource H2}" Margin="0,0,0,10"/>
                                <DataGrid x:Name="dgSkillHistory" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" 
                                          GridLinesVisibility="Horizontal" AlternatingRowBackground="#F9F9F9" 
                                          SelectionMode="Single" MinHeight="200">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="日付" Binding="{Binding RecordedDate, StringFormat='yyyy/MM/dd'}" Width="120"/>
                                        <DataGridTextColumn Header="スキル" Binding="{Binding SkillName}" Width="150"/>
                                        <DataGridTextColumn Header="レベル" Binding="{Binding SkillLevel}" Width="80"/>
                                        <DataGridTextColumn Header="変更理由" Binding="{Binding ChangeReason}" Width="*"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="現在のスキルレベル" Style="{StaticResource H2}" Margin="0,0,0,10"/>
                                <ListBox x:Name="lstCurrentSkills" BorderThickness="0" Background="Transparent" MinHeight="200">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource GoalCardStyle}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{Binding SkillName}" FontWeight="Bold"/>
                                                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                        <TextBlock Text="Lv." Foreground="#666" Margin="0,0,5,0"/>
                                                        <TextBlock Text="{Binding SkillLevel}" FontWeight="Bold" FontSize="20" Foreground="{StaticResource PrimaryBrush}"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- パフォーマンスタブ -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconBoxIcons Kind="RegularBarChart" Width="16" Height="16" Margin="0,0,8,0"/>
                        <TextBlock Text="パフォーマンス" FontWeight="SemiBold"/>
                    </StackPanel>
                </TabItem.Header>
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20" Background="{StaticResource BackgroundBrush}">
                    <StackPanel>
                        <!-- メトリクスカード -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Style="{StaticResource MetricCardStyle}">
                                <StackPanel>
                                    <TextBlock Text="タスク完了率" Foreground="#666" Margin="0,0,0,5"/>
                                    <TextBlock x:Name="txtTaskCompletionRate" Text="0%" FontSize="34" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1" Style="{StaticResource MetricCardStyle}">
                                <StackPanel>
                                    <TextBlock Text="平均集中度" Foreground="#666" Margin="0,0,0,5"/>
                                    <TextBlock x:Name="txtFocusLevel" Text="0.0" FontSize="34" FontWeight="Bold" Foreground="{StaticResource InfoBrush}"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="2" Style="{StaticResource MetricCardStyle}">
                                <StackPanel>
                                    <TextBlock Text="ヘルプ提供回数" Foreground="#666" Margin="0,0,0,5"/>
                                    <TextBlock x:Name="txtHelpCount" Text="0" FontSize="34" FontWeight="Bold" Foreground="{StaticResource WarningBrush}"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="パフォーマンスメトリクス履歴" Style="{StaticResource H2}" Margin="0,0,0,10"/>
                                <DataGrid x:Name="dgPerformanceMetrics" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal" AlternatingRowBackground="#F9F9F9" SelectionMode="Single" MinHeight="300">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="日付" Binding="{Binding MetricDate, StringFormat='yyyy/MM/dd'}" Width="120"/>
                                        <DataGridTextColumn Header="完了率" Binding="{Binding TaskCompletionRate, StringFormat='P0'}" Width="100"/>
                                        <DataGridTextColumn Header="集中度" Binding="{Binding FocusLevelAvg, StringFormat='F2'}" Width="100"/>
                                        <DataGridTextColumn Header="完了数" Binding="{Binding TasksCompleted}" Width="80"/>
                                        <DataGridTextColumn Header="ヘルプ" Binding="{Binding HelpCount}" Width="80"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- 貢献記録タブ -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconBoxIcons Kind="RegularStar" Width="16" Height="16" Margin="0,0,8,0"/>
                        <TextBlock Text="貢献記録" FontWeight="SemiBold"/>
                    </StackPanel>
                </TabItem.Header>
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20" Background="{StaticResource BackgroundBrush}">
                    <StackPanel>
                        <Border Style="{StaticResource Card}" Margin="0,0,0,15">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <TextBlock Text="貢献一覧" Style="{StaticResource H2}" VerticalAlignment="Center"/>
                                    <Button x:Name="btnRefreshContributions" Content="更新" Style="{StaticResource SecondaryButton}" Width="80" Height="35" Margin="15,0,0,0" Click="BtnRefreshContributions_Click"/>
                                </StackPanel>
                                <ListBox x:Name="lstContributions" BorderThickness="0" Background="Transparent" MinHeight="400">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource GoalCardStyle}">
                                                <StackPanel>
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                                        <TextBlock Text="{Binding Title}" FontWeight="Bold" Margin="0,0,10,0"/>
                                                        <Border Background="{StaticResource SuccessBrush}" CornerRadius="3" Padding="5,2">
                                                            <TextBlock Text="{Binding ImpactLevel}" Foreground="White" FontSize="12" FontWeight="Bold"/>
                                                        </Border>
                                                    </StackPanel>
                                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Margin="0,0,0,5"/>
                                                    <TextBlock Text="{Binding ContributionDate, StringFormat='yyyy/MM/dd'}" FontSize="13" Foreground="#999"/>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- 成長目標タブ -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconBoxIcons Kind="RegularTarget" Width="16" Height="16" Margin="0,0,8,0"/>
                        <TextBlock Text="成長目標" FontWeight="SemiBold"/>
                    </StackPanel>
                </TabItem.Header>
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20" Background="{StaticResource BackgroundBrush}">
                    <StackPanel>
                        <Border Style="{StaticResource Card}" Margin="0,0,0,15">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <TextBlock Text="進行中の目標" Style="{StaticResource H2}" VerticalAlignment="Center"/>
                                    <Button x:Name="btnRefreshGoals" Content="更新" Style="{StaticResource SecondaryButton}" Width="80" Height="35" Margin="15,0,0,0" Click="BtnRefreshGoals_Click"/>
                                </StackPanel>
                                <ListBox x:Name="lstActiveGoals" BorderThickness="0" Background="Transparent" MinHeight="300">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource GoalCardStyle}">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding GoalTitle}" FontWeight="Bold" Margin="0,0,0,5"/>
                                                    <TextBlock Text="{Binding GoalDescription}" TextWrapping="Wrap" Margin="0,0,0,10"/>
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                                        <TextBlock Text="対象スキル: " Foreground="#666"/>
                                                        <TextBlock Text="{Binding TargetSkill}" FontWeight="Bold"/>
                                                        <TextBlock Text=" → Lv." Foreground="#666" Margin="5,0,2,0"/>
                                                        <TextBlock Text="{Binding TargetLevel}" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                                                    </StackPanel>
                                                    <ProgressBar Value="{Binding Progress}" Maximum="100" Height="8" Margin="0,5,0,5" Foreground="{StaticResource PrimaryBrush}"/>
                                                    <TextBlock Text="{Binding Progress, StringFormat='進捗: {0}%'}" FontSize="13" Foreground="#666"/>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="完了した目標" Style="{StaticResource H2}" Margin="0,0,0,10"/>
                                <ListBox x:Name="lstCompletedGoals" BorderThickness="0" Background="Transparent" MinHeight="200">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource GoalCardStyle}" Background="#F8F9FA">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding GoalTitle}" FontWeight="Bold" Margin="0,0,0,5"/>
                                                    <TextBlock Text="{Binding CompletedAt, StringFormat='完了日: yyyy/MM/dd'}" FontSize="13" Foreground="{StaticResource SuccessBrush}"/>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- AI分析タブ -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconBoxIcons Kind="RegularBrain" Width="16" Height="16" Margin="0,0,8,0"/>
                        <TextBlock Text="AI分析" FontWeight="SemiBold"/>
                    </StackPanel>
                </TabItem.Header>
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20" Background="{StaticResource BackgroundBrush}">
                    <StackPanel>
                        <Border Style="{StaticResource Card}" Margin="0,0,0,15">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <TextBlock Text="強み分析" Style="{StaticResource H2}" VerticalAlignment="Center"/>
                                    <Button x:Name="btnAnalyzeStrengths" Content="分析実行" Style="{StaticResource PrimaryButton}" Width="100" Height="35" Margin="15,0,0,0" Click="BtnAnalyzeStrengths_Click"/>
                                </StackPanel>
                                <TextBlock x:Name="txtStrengthsSummary" TextWrapping="Wrap" Margin="0,0,0,10"/>
                                <ListBox x:Name="lstStrengths" BorderThickness="0" Background="Transparent" MinHeight="200">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource GoalCardStyle}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0">
                                                        <TextBlock Text="{Binding SkillName}" FontWeight="Bold" Margin="0,0,0,5"/>
                                                        <TextBlock Text="{Binding Evidence}" TextWrapping="Wrap" FontSize="14" Foreground="#666"/>
                                                    </StackPanel>
                                                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                                        <TextBlock Text="{Binding Score, StringFormat='F1'}" FontSize="26" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}" HorizontalAlignment="Center"/>
                                                        <TextBlock Text="スコア" FontSize="12" Foreground="#666" HorizontalAlignment="Center"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Card}" Margin="0,0,0,15">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <TextBlock Text="成長機会" Style="{StaticResource H2}" VerticalAlignment="Center"/>
                                    <Button x:Name="btnSuggestOpportunities" Content="提案取得" Style="{StaticResource PrimaryButton}" Width="100" Height="35" Margin="15,0,0,0" Click="BtnSuggestOpportunities_Click"/>
                                </StackPanel>
                                <ListBox x:Name="lstOpportunities" BorderThickness="0" Background="Transparent" MinHeight="200">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource GoalCardStyle}">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" Margin="0,0,0,5"/>
                                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Margin="0,0,0,5"/>
                                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                                        <TextBlock Text="{Binding SkillToImprove}" FontWeight="Bold" Foreground="{StaticResource InfoBrush}"/>
                                                        <TextBlock Text=" | " Foreground="#666" Margin="5,0"/>
                                                        <TextBlock Text="{Binding EstimatedWeeks}" FontWeight="Bold"/>
                                                        <TextBlock Text="週間" Margin="2,0,0,0"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <TextBlock Text="AI目標提案" Style="{StaticResource H2}" VerticalAlignment="Center"/>
                                    <Button x:Name="btnSuggestGoals" Content="提案取得" Style="{StaticResource PrimaryButton}" Width="100" Height="35" Margin="15,0,0,0" Click="BtnSuggestGoals_Click"/>
                                </StackPanel>
                                <ListBox x:Name="lstGoalSuggestions" BorderThickness="0" Background="Transparent" MinHeight="200">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource GoalCardStyle}">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" Margin="0,0,0,5"/>
                                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Margin="0,0,0,5"/>
                                                    <TextBlock Text="{Binding Reasoning}" TextWrapping="Wrap" FontStyle="Italic" Foreground="#666" FontSize="13" Margin="0,5,0,0"/>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="Transparent" Padding="10" Margin="0,5,0,0">
            <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconBoxIcons Kind="RegularInfoCircle" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock x:Name="txtStatus" Text="準備完了" VerticalAlignment="Center" FontSize="16"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>